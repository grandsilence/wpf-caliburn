<Window x:Class="WpfCaliburn.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfCaliburn.Views"
        xmlns:vm="clr-namespace:WpfCaliburn.ViewModels"
        mc:Ignorable="d"

        d:DataContext="{d:DesignInstance Type=vm:LoginViewModel, IsDesignTimeCreatable=True}"

        Title="Авторизация"  Width="450" Height="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <!--<RowDefinition Height="*"></RowDefinition>-->
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Label>Логин</Label>
            <TextBox x:Name="Login"/>

            <Label>Пароль</Label>
            <TextBox x:Name="Password"/>

            <!-- TODO toggle -->
            <Label Visibility="Hidden">Загрузка...</Label>

            <Button x:Name="Authenticate">Войти</Button>
        </StackPanel>
    </Grid>
</Window>
